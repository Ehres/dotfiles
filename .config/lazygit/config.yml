customCommands:
  - key: "<c-g>"
    context: "files"
    description: "Generate AI commit message"
    command: "claude -p --model haiku '/gen-commit'"
    output: terminal
    loadingText: "Generating commit message..."
