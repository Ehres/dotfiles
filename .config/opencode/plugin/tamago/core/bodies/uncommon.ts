import type { Bodies } from "../bodies.ts";
import type { SpeciesId } from "../species.ts";

/** The uncommon Species: marked silhouettes. Filled one Species at a time; a test fails while a Species of the table has no body. */
export const UNCOMMON: Record<SpeciesId, Bodies> = {};
